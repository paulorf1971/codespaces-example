name: 12 - Outputs
on:
    workflow_dispatch:
        inputs:
            build-status:
                type: choice
                options:
                    - success
                    - failure
                default: success
jobs:
    build:
        runs-on: ubuntu-latest        
        outputs: 
            build-status: ${{ steps.build.outputs.status }}        
        steps:
            - name: Print GITHUB_OUTPUT path
              run: echo $GITHUB_OUTPUT
            - id: build
              name: Build
              run: echo "status=${{inputs.build-status}}" >> "$GITHUB_OUTPUT"
    deploy:
        runs-on: ubuntu-latest
        needs: build        
        steps:
            - name: Deploy              
              if: ${{ needs.build.outputs.build-status == 'success' }}
              run: echo "Deploying, build was ${{ needs.build.outputs.build-status || 'failed' }}"
            
