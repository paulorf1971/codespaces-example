name: 05-2 Activity Type
on: 
    pull_request:
        types:
            - closed
        branches:
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Create activity type
              run: echo "Running whenever a PR is opened and synchronized AND base branch is main"
