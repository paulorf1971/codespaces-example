name: 10 - Execution Flow with 'need'
on:
    workflow_dispatch:
        inputs:
            pass-unit-tests:
                type: boolean
                description: Whether the unit tests will pass or not
                default: true
                
jobs:
    lint-build:
        runs-on: ubuntu-latest
        steps:
            - name: Lint and build
              run: echo "Linting and building project"
            #   run: |
            #     npm run lint
            #     npm run tsc:check
            #     npm run build
    unit-test:
        runs-on: ubuntu-latest
        # This will make the flow continue event with an error
        continue-on-error: true
        steps:
            - name: Running unit tests
              run: echo "Running tests..."

            - name: Failing tests
              if: ${{ !inputs.pass-unit-tests }}
              run: |
                echo "Running tests failed"
                exit 1

            - name: Success tests
              if: ${{ inputs.pass-unit-tests }}
              run: echo "Running tests success"
    deploy-nonprod:
        runs-on: ubuntu-latest
        needs: 
            - lint-build
            - unit-test
        steps:
            - name: Deploying to Non-Prod
              run: echo "Deploying to non prod..."

    e2e-tests:
        runs-on: ubuntu-latest
        needs: 
            - deploy-nonprod
        steps:
            - name: Running E2E tests
              run: echo "Running e2e tests"

    load-tests:
        runs-on: ubuntu-latest
        needs: 
            - deploy-nonprod
        steps:
            - name: Running load tests
              run: echo "Running load tests"

    deploy-prod:
        runs-on: ubuntu-latest
        needs: 
            - e2e-tests
            - load-tests
        steps:
            - name: Deploying to prod
              run: echo "Deploying to prod"
    

